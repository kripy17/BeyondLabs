# Workbench components

Use `WorkbenchPage`, `WorkbenchHeader`, and `WorkbenchPanel` from `../layout/WorkbenchShell.jsx`
for consistent page layouts across all routed workspaces.

Pages compose the same layout rhythm: `WorkbenchPage` → `WorkbenchHeader` → panels/cards → results.
